{
  "name": "dlt",
  "display_name": "DLT logging",
  "version": "2.0.0",
  "type": "adapter",
  "description": "Capture automotive DLT logs — either by calling your dlt.py bench script (start/stop/capture) or via the built-in TCP capture when no script is set — then verify patterns and attach the .dlt to the report.",
  "powered_by": { "tool": "your dlt.py script (or built-in TCP capture)" },
  "capabilities": ["start_capture", "stop_capture", "capture", "save_file", "verify_pattern", "tail"],
  "capability_groups": {
    "Capture": ["start_capture", "stop_capture", "capture"],
    "Analyse": ["verify_pattern", "tail", "save_file"]
  },
  "dependencies": [],
  "entry_point": "adapter:DLTAdapter"
}
