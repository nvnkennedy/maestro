# Maestro backend dependencies (pip install -r requirements.txt)
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.5
apscheduler>=3.10,<4
paramiko>=2.11
cryptography>=42.0
websockets>=11.0

# Device tooling — the adb/ssh adapters call your own tools
turboadb>=1.0.14
turbossh>=1.2.31

# Optional (graceful fallbacks exist when missing)
structlog>=24.1
prometheus-client>=0.20
pyserial>=3.5

# Development / testing
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
